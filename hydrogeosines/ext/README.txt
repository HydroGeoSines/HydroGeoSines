# Naming
- Affiliation_(Attribute,Method,Function,Extension)
- e.g. pandas_hgs is a extension specifically affiliated to the pandas package
- time.py could also be hgs_time.py if the methods of both classes are specific to hgs (needs to be seen and discussed).
- hgs_analysis is specific to the hgs package and an extension thereof.